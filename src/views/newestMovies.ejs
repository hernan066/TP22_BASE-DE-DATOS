<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Películas ordenas por fecha</title>
    <link rel='stylesheet' href='/css/style.css' />
</head>
<body>
    <%- include ('./partials/header') %>
    <main>

   
    <h1>LAS MAS NUEVAS</h1>
    <ul>
        <% for( let i = 0; i < movies.length; i++ ) { %>
        <li>
            <strong> <a href="/movies/detail/<%=movies[i].id%>"><%= movies[i].title %> </a></strong> <br>
            <%= 'Estreno: ' + moment(movies[i].release_date).format('DD-MM-YYYY') %> 
        </li>
        <% } %>
        
    </ul>
</main>
</body>
</html>